export const BINDER_SIZE = 60;
export const CARDS_PER_PAGE = 12;
export const BINDER_PAGE_COUNT = Math.ceil(BINDER_SIZE / CARDS_PER_PAGE);
export const MAX_BINDER_PAGE_INDEX = BINDER_PAGE_COUNT - 1;

export const TABLEAU_SIZE_LIMIT = 99;
export const PAGE_CHANGE_INTERVAL = 500;